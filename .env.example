# Optional: GitHub token (no scopes needed) to avoid the 60 req/h API rate limit.
GITHUB_TOKEN=

# Public site URL used for canonical links, sitemap and OG tags.
# Falls back to Vercel's production domain, then http://localhost:3000.
NEXT_PUBLIC_SITE_URL=
